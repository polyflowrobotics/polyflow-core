{
  "type": "input",
  "name": "Gamepad",
  "entry_point": "gamepad/node.py",
  "topics": [
    {
      "name": "axes",
      "direction": "publish",
      "msg_type": "polyflow_msgs/GamepadAxes",
      "description": "Current normalized stick and trigger positions from the selected gamepad, after the deadzone is applied."
    },
    {
      "name": "buttons",
      "direction": "publish",
      "msg_type": "polyflow_msgs/GamepadButtons",
      "description": "Current pressed state of every button on the gamepad."
    },
    {
      "name": "cmd_vel",
      "direction": "publish",
      "msg_type": "geometry_msgs/Twist",
      "description": "Velocity command derived from the sticks. Shape depends on output_mode — diff_drive fills only linear.x + angular.z; 6dof maps all four sticks plus shoulder/trigger buttons across the full Twist."
    }
  ],
  "services": [],
  "actions": [],
  "parameters_schema": {
    "device_index": {
      "label": "Device Index",
      "type": "number",
      "default": 0,
      "description": "Gamepad device index.",
      "ui": { "group": "Gamepad", "order": 1 }
    },
    "poll_rate_hz": {
      "label": "Poll Rate",
      "type": "number",
      "units": "Hz",
      "default": 60,
      "description": "How often to poll the gamepad.",
      "ui": { "group": "Gamepad", "order": 2 }
    },
    "deadzone": {
      "label": "Deadzone",
      "type": "number",
      "default": 0.05,
      "description": "Axis deadzone threshold.",
      "ui": { "group": "Gamepad", "order": 3 }
    },
    "output_mode": {
      "label": "cmd_vel Mapping",
      "type": "string",
      "default": "diff_drive",
      "allowed_values": ["diff_drive", "6dof"],
      "description": "diff_drive fills only linear.x + angular.z (for differential-drive bases). 6dof maps all four sticks plus shoulder/trigger buttons across the full Twist (for pose-controlled arms / IK). Output is normalized to [-1, 1] either way.",
      "ui": { "group": "Velocity", "order": 1 }
    }
  },
  "defaults": {
    "rate_hz": 60
  }
}
